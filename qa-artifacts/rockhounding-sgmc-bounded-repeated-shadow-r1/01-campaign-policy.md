# Campaign policy

Frozen in `USGS_SGMC_REPEATED_SHADOW_CAMPAIGN_POLICY`.

Mode `SHADOW_ONLY`. Adapter 1.0.0. Operation `AUTOMATED_QUERY`. Layer `SGMC_Geology` id 3. Maximum 4 queries. Maximum envelope 0.02 degrees per side. Maximum 5 features. Parallelism 1. No automatic retry, pagination, background polling, or public materialization. Production authority `NONE`. Governance, disclosure, and admission are per request. Off-switch is governance suspension.

This policy authorizes the campaign only.
