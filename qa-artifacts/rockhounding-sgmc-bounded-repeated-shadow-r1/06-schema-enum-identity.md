# Schema, enum, and identity

Successful reads returned the pinned attributes plus `OBJECTID`. No additional attribute appeared. Field presence is `MATCH`.

Geometry type stayed polygonal rings. Spatial reference stayed `wkid` 102100 with `latestWkid` 3857. Campaign geometry stability: `STABLE`. CRS stability: `STABLE`.

Lithology on the successful reads was in the pinned set. Enum stability: `KNOWN_SET_OBSERVED`. No mapping was extended.

Ages stayed geologic text. Temporal semantics: `STABLE`. Ages were not treated as Truth Clock timestamps.

Scoped identity components were present and not duplicated inside a read. Identity stability: `STABLE_WITH_SCOPE`. `OBJECTID` remains a service handle.

Schema stability: `STABLE`.
