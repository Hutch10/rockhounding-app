# 01 Red-first

`pnpm exec vitest run packages/shared/src/provenance-activity-kernel.test.ts`

Failed to load `./provenance-activity-kernel` (module does not exist). Suites: 1 failed. Tests: none collected.
