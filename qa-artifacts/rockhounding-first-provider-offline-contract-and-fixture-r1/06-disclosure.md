# Disclosure

Public map-unit geometry for `SHADOW_DISPLAY` and `PUBLIC_MAP` is released only by disclosure projection.

Unknown sensitivity is `UNKNOWN_FAIL_CLOSED` and `WITHHELD`.

Materializing a raw geometry object without a disclosure projection is rejected.

No provider geometry is displayed.
