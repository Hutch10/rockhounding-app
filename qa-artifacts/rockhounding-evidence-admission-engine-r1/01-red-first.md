# Evidence Admission Engine R1 — red-first

Command:

`pnpm exec vitest run packages/shared/src/evidence-admission.test.ts`

Result: exit 1. The suite failed to load because `./evidence-admission` did not exist.

Baseline before the suite was 60 files / 781 tests.
