# Raw response retention

The raw feature response, service metadata, and layer metadata are local files in this directory. Redistribution and caching remain UNKNOWN, so those bytes are gitignored. The committed integrity record is the SHA-256 in `../campaign-summary.json`.
