# Regression

`pnpm test:ci` exit 0: 69 files, 860 tests. Shared type-check, shared build, and `build:core` exit 0. Targeted eslint on the shadow modules exit 0.

Web production build exit 0. Root `pnpm lint` exit 1: 285 problems (271 errors, 14 warnings). That debt is pre-existing. No SGMC shadow file is in it.
