# Disclosure

`PUBLIC` + `SHADOW_DISPLAY` is `ALLOWED`. Materialization stayed in QA scope. No ordinary product surface received the geometry. Unknown sensitivity remains withheld by the disclosure tests.
