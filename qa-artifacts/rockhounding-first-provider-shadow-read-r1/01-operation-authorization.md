# Operation authorization

Authorization ran before transport. Status `ALLOWED_WITH_CONSTRAINTS` for `AUTOMATED_QUERY`. A read-only receipt, suspended governance, a tolerant adapter, and a missing precondition block the transport. The off-switch after capture returned `AUTHORIZATION_BLOCKED` and did not open a second request.
