# Live versus offline contract

Comparison marks are in `campaign-summary.json`. Source, layer, geometry, spatial reference, core fields, field types, lithology, ages, identity components, pagination, and max record count are MATCH. Unknown extra fields were not returned. Coverage semantics stay UNKNOWN. `materialDrift` is false. The identity scope is still not a global unique key.
