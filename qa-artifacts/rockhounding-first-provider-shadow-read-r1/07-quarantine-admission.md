# Quarantine and admission

The live record translated with adapter 1.0.0 as `SUCCESS`. No quarantine was required for this response. Admission status `ADMITTED` for geological context. Confirmed absence is false. Tests still quarantine schema drift, unknown lithology, and a non-text age without using them as truth-clock fields.
