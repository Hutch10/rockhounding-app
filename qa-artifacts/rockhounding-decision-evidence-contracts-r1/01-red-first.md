# Decision Evidence Contracts R1 — red-first

Command:

`pnpm exec vitest run packages/shared/src/decision-evidence-contracts.test.ts`

Result: exit 1. The suite failed to load because `./decision-evidence-contracts` did not exist.

Baseline before the suite was 61 files / 797 tests.
