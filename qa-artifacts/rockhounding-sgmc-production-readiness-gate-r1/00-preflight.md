# Preflight

Branch `feat/sprint-4-field-mode`. HEAD and origin were both `bb549d86089a70c8d8b1b7f136f2308092a1ac60`.

Prior result: `ROCKHOUNDING_SGMC_BOUNDED_REPEATED_SHADOW_R1_PASS`.

Provider identity, adapter 1.0.0, and `AUTOMATED_QUERY` match the certified profile. Production ingestion is CLOSED. Ordinary-user display is CLOSED. Live authority is `BOUNDED_REPEATED_SHADOW_READ`.

Campaign evidence: 4 planned, 4 executed, 2 HTTP 200, 2 HTTP 504. The 504s were not retried, not treated as zero geology, not admitted, and not promoted. The HTTP 200 reads replayed deterministically.

Unrelated dirty paths were left unstaged. No new feature query was issued.
