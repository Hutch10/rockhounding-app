# Off-switch and rollback

`suspendSgmcGovernance` already blocks prospective transport. The repeated-shadow campaign confirmed that block before and after the reads, with no provider call on the probe.

Rollback disables new live presentation, retains provenance, and does not rewrite receipts or infer an empty geology state. Disabling the provider does not require a database migration.

Schema drift quarantines and stops promotion. The adapter is not revised inside an incident.
