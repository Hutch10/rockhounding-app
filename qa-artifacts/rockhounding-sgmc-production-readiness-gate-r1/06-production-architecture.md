# Production architecture

Required path:

governed transport, raw response, adapter 1.0.0, quarantine, admission, disclosure, geological-context projection.

The browser does not call the feature service. The endpoint, layer 3, fields, bbox, and result limit are fixed. DOI `10.5066/F7WH2N65` stays pinned. DOI `10.5066/P1A3DQZK` is not substituted.

SGMC unavailability does not stop unrelated product workflows. No production surface is enabled by this gate.
