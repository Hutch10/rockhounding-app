# Disclosure and governance

Disclosure ran before QA materialization and returned `ALLOWED` for `PUBLIC` / `SHADOW_DISPLAY`. Unknown sensitivity stays withheld. Product surfaces stay closed. Governance was `ADMITTED` and `REVIEWED` for the live read. Repeated shadow mode must re-evaluate authorization before every transport call. `suspendSgmcGovernance` blocks the next call without deleting the artifact.
