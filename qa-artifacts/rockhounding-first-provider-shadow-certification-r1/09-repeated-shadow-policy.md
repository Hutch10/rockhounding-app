# Repeated shadow policy

Mode `SHADOW_ONLY`. Adapter 1.0.0. Operation `AUTOMATED_QUERY`. Layer `SGMC_Geology` only. One envelope per request, at most 0.02 degrees on a side, at most 5 features, pinned outFields. Manual trigger, one request per action, no polling, no fan-out, no automatic retry. Single page. Governance before every request. Disclosure before every materialization. Admission after every promotable candidate. Quarantine on material drift. Production authority none. Raw storage is local QA only. Off-switch is governance suspension.
