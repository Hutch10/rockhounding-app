# Governance, admission, disclosure

Suspended governance and a READ-only grant do not call the provider. A denied public-display grant, unknown disclosure, and a rejected admission hide units. Query permission does not display by itself.
