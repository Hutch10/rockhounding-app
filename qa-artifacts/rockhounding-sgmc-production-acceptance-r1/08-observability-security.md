# Observability and security

The observation object records outcome, failure class, latency, feature count, quarantine, admission failure, and disclosure or display blocks. Oversized and malformed boxes are rejected before transport. The endpoint is not a caller argument.
