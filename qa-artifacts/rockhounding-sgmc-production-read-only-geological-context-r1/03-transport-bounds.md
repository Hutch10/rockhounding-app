# Transport

The certified shadow GET path is reused. Endpoint and layer are fixed. Oversized boxes are rejected. One logical request makes at most one provider call. HTTP 504 and timeout become provider unavailable. Alaska and Hawaii do not call the provider.
