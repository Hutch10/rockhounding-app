# Production contract

Profile `PRODUCTION_READ_ONLY_GEOLOGICAL_CONTEXT`. Adapter 1.0.0. Purpose `GEOLOGICAL_CONTEXT`. Query `AUTOMATED_QUERY`. Display `PUBLIC_DISPLAY`. Max box 0.02 by 0.02 degrees. Max 5 features. No retry, pagination, polling, durable cache, public API, offline package, or redistribution.
