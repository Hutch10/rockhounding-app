# Off-switch

A suspended governance request returns unavailable and does not call transport. The product flag is subordinate to that check. Disabling the flag stops new Site Detail requests. No database migration.
