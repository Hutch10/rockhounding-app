# 01 Red-first

`pnpm exec vitest run packages/shared/src/observation-sample-model.test.ts`

Failed to load `./observation-sample-model` (module does not exist). Suites: 1 failed. Tests: none collected.
